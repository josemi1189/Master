import { MemberDetail } from "@/pods";
import { AppLayout } from "@/layout";

export const MemberDetailScene: React.FC = () => {
  return (
    <AppLayout>
      <MemberDetail />
    </AppLayout>
  );
};
