import { AppLayout } from "@/layout";
import { MemberListComponent } from "@/pods/member-list";

export const MembersScene = () => {
  return (
    <AppLayout>
      <MemberListComponent />
    </AppLayout>
  );
};
